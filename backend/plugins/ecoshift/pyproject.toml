[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecoshift"
version = "0.1.0"
description = "EcoShift Scheduler plug-in for CarbonCore"
requires-python = ">=3.9"
dependencies = ["fastapi", "sqlalchemy"]
