[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "carboncomply"
version = "0.1.0"
description = "CarbonComply ESRS export plug-in for CarbonCore"
requires-python = ">=3.9"
dependencies = ["fastapi", "pandas", "sqlalchemy", "openpyxl"]
