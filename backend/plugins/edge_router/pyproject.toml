[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edge_router"
version = "0.1.0"
description = "Edge Router plugin"
requires-python = ">=3.9"
dependencies = ["fastapi"]
